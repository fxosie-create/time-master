import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.ymrlab.timemaster",
  appName: "時間マスター",
  webDir: "out",
  android: {
    backgroundColor: "#f7f8ff",
  },
};

export default config;
